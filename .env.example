# APS credentials — local token server only (never commit real values)
APS_CLIENT_ID=
APS_CLIENT_SECRET=

# Written into public/config.json for local dev (npm run config)
MODEL_URN=
TOKEN_URL=/api/token
APS_ENV=AutodeskProduction

# GitHub Pages base path (repo name)
VITE_BASE_PATH=/prototype-environment-clean/
